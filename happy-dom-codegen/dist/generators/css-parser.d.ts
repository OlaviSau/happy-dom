/**
 * Generates CSSStyleDeclarationCSSParser.ts — CSS text → property declarations.
 * This is a static utility (no IR dependency), ported from happy-dom.
 */
export declare function generateCSSParser(): string;
